$ git add . 
$ git commit -m 'first_commit'
$ git remote add origin git@github.com:longqimao/www.git
$ git push -u origin master
if fatal: remote origin already exists;
$ git remote rm origin 
$ git pull origin master
